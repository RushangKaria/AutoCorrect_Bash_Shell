Simple script to perform autocorrection of typed commands in the BASH Shell

If there is no file containing command names it by default extracts
the names from the bin and other folders.

This operation if done in this way will be very slow, ranging to the order of seconds